# Manual Testing Discovery

This file contains a bug and room for improvement found by manually reviews and uses the tool

1. Improve on UI for the tab that is not process as its very hard to read
2. Use icon.png as the icon of the application / tool
3. The UI for all the tab other than process does not have horizontal scrolling so when the data is too long, can not read
4. Add right click on the field to let player copy the data in specific colume they want
5. Timestamp display in tab only show HH:mm:ss:xxx not full timestamp YYYY-MM-DD HH:mm:ss:xxx
6. Drivers tab, could it changed to image loaded? for more accurary?