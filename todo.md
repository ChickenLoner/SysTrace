- Add export for generated Timeline, and remove “Export” from “File” menu, it serve no purpose
- Multi Host support (maybe a separate tab with the hostname as tab name and user can change it)
- More stats like most Connected IP address

## Create more sigma rule to ship with the tool
Need sysmon log that contain several attacks includes
- 